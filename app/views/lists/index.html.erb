
  <div class="container">
    <h3>My Lists</h3>
      <ul>
        <% @lists.each do |list| %>
        <li><%= link_to list.name, list_path(list) %></li>

        <% end %>
    </ul>

     </div>
<%= link_to 'Create new list', new_list_path, class: 'btn btn-flat' %>
